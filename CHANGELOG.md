# 版本记录

本项目在首个公开版本前使用过 2.x、1.0.1 和 1.0 等内部里程碑标签。这些标签从未对外分发，不代表已发布的 SemVer 序列；当前发布线从 `1.0.0-rc.1` 开始。

## 1.0.0-rc.1（待发布）

### 新增

- Popup 与 Options 采用统一设计系统、响应式布局和系统深色模式。
- Popup 保留全部站点的独立滚动列表，提供健康、分类、标签和搜索筛选。
- 批量余额刷新提供协作式停止与继续，保留已完成结果和未处理队列。
- Options 按站点管理、导入与备份、诊断与权限划分工作区。
- 导入必须先预览新增、更新和跳过数量。
- 提供手动复制的脱敏诊断，不包含遥测或自动上传。
- 提供无账号、无 Key 的真实扩展上下文冒烟门禁。

### 安全与正确性

- 站点权限必须在用户手势仍有效的前台点击处理器中请求。
- 批量刷新即使有站点拒绝授权，也继续处理已授权站点并将未授权项记为跳过。
- 完整 Key 不进入普通列表、诊断复制、日志或验收材料。
- Origin 保留显式端口；站点 URL 保存前移除 query 和 hash。
- 完整导出、替换导入、恢复和清理均要求明确确认。

### 发布门禁

- 隔离 Edge/Chromium 扩展上下文自动冒烟必须通过；Chrome Stable 与 Edge Stable 必须另行完成双人真实验收。
- 两名测试者必须使用相互独立的专用测试账号和 Key，并完成五天交叉浏览器验收。
- P0、P1 和阻断型 P2 均为零后，维护者才能签署发布。

## 首发前内部里程碑（未分发）

### 2.4.6

- 诊断增加“打开未登录站”，只处理登录类余额失败。
- 抽出 `balance-refresh.js` 负责单站、批量和失败重试编排。

### 2.4.5

- 诊断增加“打开失败站”，默认最多打开五个站点。
- 抽出 `site-tabs.js` 负责临时标签、同 Origin 复用和安全关闭。

### 2.4.4

- 诊断增加“重试余额失败”。
- 抽出 `permissions.js` 负责可选主机权限和未授权统计。
- 增加经典脚本加载顺序与核心 API 冒烟测试。

### 2.4.3

- 诊断增加“授权未授权站”，授权后不自动查询余额。
- 抽出 `page-scrape.js` 负责页面余额和 Key 抓取。

### 2.4.2

- 诊断展示已授权和未授权站点数。
- 抽出 `tab-api-key.js` 负责标签页账号验证和创建 Key。

### 2.4.1

- 增加脱敏导出和本机只读诊断。
- 抽出 `balance-format.js` 负责解析与稳定错误分级。

### 2.4.0

- 自动创建 Key 默认改为有限额度和 90 天有效期；高风险无限配置移入显式设置。
- 签到模块退出关键路径。
- 站点数据采用有序 schema 迁移。

### 2.3.3

- 余额失败持久化稳定错误码和建议动作。
- 增加打开站点、重新识别、重试并授权和再次查询入口。
- 单站余额查询增加 25 秒硬超时。

### 2.3.2

- 批量余额先处理已授权站点，拒绝授权的站点记为跳过，不中断其余站点。
- NewAPI/未识别站点与其他类型站点使用不同的无 Key 操作文案。

### 2.3.1

- 重新识别纳入按站点授权流程。
- 自动获取 Key 只有在确实需要创建时才要求确认。
- 签到消息接口硬下线，保持产品边界独立。

### 2.3.0

- 站点访问改为按需授权。
- 批量余额保存未完成队列，支持中断后继续。
- 增加 schemaVersion、站点多标签和更宽松的 Popup 卡片布局。

### 2.2.0

- 完整 Key 导出增加风险确认。

### 2.1

- 打开站点固定进入站点首页，避免误入控制台深路径。
- 余额支持站点 `quota_per_unit`，查询不改写用户当前标签页。
- 收藏时可从已登录站点导入已有完整 Key。
- 自动获取 Key 只在用户主动操作时运行，并先尝试导入已有 Key。

### 1.0.1

- 增加标签筛选、较短的余额失败文案和更紧凑的卡片密度。

### 1.0

- Popup 增加余额失败站点快捷处理入口。
- 抽出 `key-import.js` 负责自动导入、合并和获取 Key 编排。
